# Pre-submission TODO list

State as of 2026-06-10. The V3 packaging step and ARM-TTK validation are now done locally; this list tracks the placeholders + assets that must resolve before the PR is taken out of DRAFT. Most are owned by Jamie via Partner Center or the Bindplane brand team.

## SolutionMetadata.json

| Field | Current value | Owner | Notes |
|---|---|---|---|
| `publisherId` | `TODO_BINDPLANE_PUBLISHER_ID` | Jamie / Partner Center | Auto-generated by Partner Center on offer creation (looks like `<companyname><timestamp>` per Cribl's `criblinc1673975616879`). |
| `support.email` | `TODO_BINDPLANE_SUPPORT_EMAIL` | Bindplane support | Public support inbox surfaced on the Content Hub listing. |
| `support.link` | `https://bindplane.com/support` | Bindplane | Confirm canonical URL. |
| `firstPublishDate` / `lastPublishDate` | `2026-06-10` | — | Refresh `lastPublishDate` to the actual merge date before submission. |

## Data/Solution_Bindplane.json

| Field | Current value | Owner | Notes |
|---|---|---|---|
| `Author` | `Bindplane - TODO_BINDPLANE_SUPPORT_EMAIL` | Bindplane support | Mirrors `SolutionMetadata.support.email`. |
| `Version` | `3.0.1` | — | V3 bumped from `3.0.0`. The `Package/3.0.1.zip` and `mainTemplate.json` reflect this. |
| `BasePath` | `C:\GitHub\Azure-Sentinel\Solutions\Bindplane` | — | V3 expects a Windows-style path. Leaving as-is (Cribl uses the same format). |

## Logos/Bindplane-Logo.svg — DONE (2026-06-11)

Replaced with the official `Bindplane-icon_black.svg` from the brand kit (the icon is designated for limited-space applications, black on light backgrounds, per Brand Guidelines p7). Also placed at `Workbooks/Images/Logos/Bindplane.svg` and referenced by the workbook entry in `Workbooks/WorkbooksMetadata.json`.

## Workbooks/Images/Preview/Bindplane{Black,White}.png — DONE (2026-06-15)

Workbook screenshots captured from the `sentinel-demo` workspace and placed at `Workbooks/Images/Preview/BindplaneBlack.png` + `BindplaneWhite.png` (names match `WorkbooksMetadata.json:previewImagesFileNames`). Captured against all ASIM data (demo workspace has no Bindplane marker yet); the shipped workbook keeps the marker filter — previews are illustrative only.

## Workbooks/Images/Logos/Bindplane.svg — DONE (2026-06-11)

Official icon placed; `Workbooks/WorkbooksMetadata.json:logoFileName` updated to `Bindplane.svg`.

## Partner Center (Jamie)

| Item | Required value | Notes |
|---|---|---|
| Offer search keyword | `f1de974b-f438-4719-b423-8bf704ba2aef` | Must be set as a search keyword on the offer or the listing will not appear in Sentinel Content Hub. |
| offerId | `microsoft-sentinel-solution-bindplane` | Must exactly match `SolutionMetadata.offerId`. Immutable once created. |
| Version | `3.0.1` | Must match `Data/Solution_Bindplane.json:Version` and `Package/mainTemplate.json` version. |

## Release dependency

The connector queries depend on the contrib `asim_standardization` processor `v1.8.0+` (`attribution_fields` + TimeGenerated auto-population). Chain state as of 2026-06-12:

1. DONE: contrib #410 (attribution_fields) and #418 (TimeGenerated auto-populate) merged to contrib main; op-enterprise #8795 + #8864 (template wiring, preset fixes, save-time validation, mapping editor UI) merged.
2. PENDING: tag `processor/asimstandardizationprocessor/v1.8.0` (next coordinated contrib module bump).
3. PENDING: a `bindplane-otel-collector` release pinning contrib `v1.8.0+` (latest is v1.101.1, pre-dates the tag) and a bta release on the same contrib version (live-preview parity).
4. Customers upgrade to that collector release.

Do not take this PR out of DRAFT until at least (2) and (3) ship.

## Done

- Forked `Azure/Azure-Sentinel` → `eKuG/Azure-Sentinel` and cloned sparse + shallow.
- Branch `bindplane-solution-v1.0.0` off `master`.
- Authored `SolutionMetadata.json`, `Data/Solution_Bindplane.json`, `Data Connectors/Connector_Bindplane.json`, `Workbooks/BindplaneWorkbook.json`, `ReleaseNotes.md`.
- Added Bindplane entry to top-level `Workbooks/WorkbooksMetadata.json`.
- Mirrored workbook to top-level `Workbooks/BindplaneWorkbook.json` (Cribl pattern).
- Placeholder logo at `Logos/Bindplane-Logo.svg`.
- Ran V3 PowerShell solution builder → produced `Package/mainTemplate.json`, `Package/createUiDefinition.json`, `Package/testParameters.json`, `Package/3.0.1.zip`.
- Ran ARM-TTK inline via V3: **48/49 passing**. The single failure is the "IDs Should Be Derived From ResourceIDs" rule on `contentProductId` / `id` properties — Cribl's merged Package uses the same pattern (`[variables('_xxxcontentProductId')]`), so this is a known false positive that the Sentinel-Solutions automation tolerates.
